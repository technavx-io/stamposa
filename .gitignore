# Dependencies
node_modules/

# Builds
dist/
.next/
out/
*.tsbuildinfo

# Environment — never commit real secrets
.env
.env.local
.env.*.local

# Runtime data
apps/api/uploads/
.local/
*.log

# Databases (if using docker volumes mapped locally)
pgdata/
redisdata/

# OS / editor
.DS_Store
.idea/
.vscode/
coverage/

# Local Netlify folder
.netlify

# Wallet dev fixtures — self-signed keys, regenerated by make-wallet-fixtures.sh
apps/api/test/fixtures/

# Production env files — never commit
.env.production
deploy/.env.production

# CI workflow — needs a workflow-scoped token to push; kept local for now
.github/workflows/

# Local editor/agent tooling (launch.json points at machine-specific paths).
.claude/
