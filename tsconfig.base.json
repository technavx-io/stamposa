{
  "$comment": "Common TypeScript options every Stamposa workspace agrees on. Workspace tsconfigs extend this and add stack-specific bits (Next.js apps also extend tsconfig.next.json). Do not add stack-specific options here — they must apply to Nest, Next.js, and library code equally.",
  "compilerOptions": {
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "incremental": true,
    "forceConsistentCasingInFileNames": true
  }
}
