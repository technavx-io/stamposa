# app-staging.stamposa.com — prebuilt Next.js standalone bundle of apps/frontend,
# rebuilt with staging NEXT_PUBLIC_* URLs baked in.
FROM node:22-alpine
ENV NODE_ENV=production PORT=3011 HOSTNAME=127.0.0.1
WORKDIR /app
COPY frontend/ ./
EXPOSE 3011
CMD ["node", "apps/frontend/server.js"]
